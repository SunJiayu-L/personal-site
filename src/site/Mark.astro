---
import { base } from './data'
interface Props {
  size?: number
}
const { size = 30 } = Astro.props
---

<img src={base + '/sujnrose.jpg'} width={size} height={size} alt='' aria-hidden='true' style='display:block;object-fit:contain;border-radius:8px;background:#fff;flex-shrink:0' />
